name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  typecheck-and-test:
    runs-on: ubuntu-latest
    strategy:
      # Tested on the LTS the project targets and the version used during
      # development. A version-specific failure (Node's --test glob support
      # differs across majors) must surface here, not in production.
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - name: Typecheck
        run: npx tsc --noEmit
      - name: Unit tests
        run: npm test

  schema:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready --health-interval 10s
          --health-timeout 5s --health-retries 5
        ports: ['5432:5432']
    steps:
      - uses: actions/checkout@v4
      - name: Apply migrations and verify tenant isolation
        env:
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          psql -h localhost -U postgres -d postgres -c 'create database aether;'
          psql -h localhost -U postgres -d aether -c 'create extension if not exists pgcrypto;'
          psql -h localhost -U postgres -d aether -f supabase/tests/00_local_auth_stub.sql
          psql -h localhost -U postgres -d aether -v ON_ERROR_STOP=1 -f supabase/migrations/0001_core_schema.sql
          echo "Migration applied cleanly."
